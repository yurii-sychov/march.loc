
<div class="form-check form-switch {{ class }}">
    <input class="form-check-input" type="checkbox" role="switch" id="{{ id }}" {% if checkedStatus %} checked{% endif %}{% if disabledStatus %} disabled{% endif %}>
    <label class="form-check-label" for="{{ id }}">{{ message }}</label>
</div>
